<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Colorful Transaction Page</title>
  <style>
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #ff6ec4, #7873f5); /* Diagonal gradient */
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      color: #fff;
    }

    .container {
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(10px);
      padding: 30px;
      border-radius: 16px;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.25);
      width: 100%;
      max-width: 600px;
    }

    .container h1 {
      font-size: 32px;
      text-align: center;
      margin-bottom: 10px;
    }

    .container p {
      text-align: center;
      font-size: 16px;
      margin-bottom: 20px;
      color: #f1f1f1;
    }

    form {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    input[type="text"],
    input[type="number"] {
      padding: 12px;
      border: none;
      border-radius: 8px;
      font-size: 16px;
      outline: none;
    }

    button {
      padding: 12px;
      background-color: #00c9a7;
      color: #fff;
      font-size: 16px;
      border: none;
      border-radius: 8px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    button:hover {
      background-color: #00a98f;
    }

    .transaction-list {
      margin-top: 25px;
      max-height: 300px;
      overflow-y: auto;
    }

    .transaction {
      background-color: rgba(255, 255, 255, 0.2);
      padding: 10px 15px;
      margin-bottom: 10px;
      border-left: 5px solid #00c9a7;
      border-radius: 6px;
      color: #fff;
      font-size: 16px;
    }

    /* Scrollbar for overflow */
    .transaction-list::-webkit-scrollbar {
      width: 8px;
    }
    .transaction-list::-webkit-scrollbar-thumb {
      background-color: rgba(255, 255, 255, 0.3);
      border-radius: 4px;
    }
  </style>
</head>
<body>




</body>
</html>
